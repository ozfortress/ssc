language: d
d:
  - dmd-2.073.2

addons:
  apt:
    packages:
    - libevent-dev
    - expect

install:
  - dub fetch doveralls

script:
  - mkdir logs
  - dub test --coverage --combined
  # TODO: Coverage
  # - rm .*.lst # hack for test coverage
  # - dub run doveralls
